1교시
9주차 수업 복습 및 코액스 관련 공지

2교시
브랜치 삭제(local)
git branch -d 브랜치이름

브랜치 삭제(remote)
git push origin --delete 브랜치이름

vi 에디터 기초
일반모드(esc),입력모드(i o a),명령행모드(:)

일반모드
x(한글자 삭제), dd(현재행 잘라내기), yy(복사), p(붙여넣기)

입력모드
편집

명령행모드
w(저장), q(종료), !(강제로)

3교시
브랜치 생성 후 이동
git checkout -b 브랜치명
git switch -c 브랜치명
